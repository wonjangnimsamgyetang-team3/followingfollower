import PostList from '@/components/TodoList';

const page = () => {
  return (
    <div className="w-full h-full flex items-center flex justify-center bg-green-500">
      <PostList />
    </div>
  );
};

export default page;
